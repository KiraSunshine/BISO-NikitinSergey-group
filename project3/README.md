# Практическая работа №3

Вариант 13 (принято соответствие 13 -> 1 из методички): **Task Manager**.


## Структура

- `cmd/app` — точка входа CLI
- `internal/domain` — модели и бизнес-логика
- `internal/patterns` — паттерны Command, Observer, Factory Method
- `internal/storage` — in-memory репозиторий
- `diagrams` — PlantUML-диаграммы
- `.gitlab-ci.yml` — конфигурация GitLab CI
- `report.docx` — пояснительная записка
